all:
    children:
        cluster:
            children:
                consul:
                    children:
                        consul-leader:
                            hosts:

                        consul-servers:
                            hosts:

                        consul-clients:
                            hosts:

                nomad:
                    children:
                        nomad-leader:
                            hosts:

                        nomad-servers:
                            hosts:

                        nomad-clients:
                            hosts:

                vault:
                    children:
                        vault-active:
                            hosts:

                        vault-standby:
                            hosts: